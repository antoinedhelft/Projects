groups:
  - name: api-alerts
    rules:
      - alert: ApiDown
        expr: up{job="api"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "API indisponible"
          description: "Le job 'api' ne répond plus depuis > 1 minute."
